# SleepMedic End-to-End Audit

Run date: 2026-04-15

## Results

| # | Check | Status | Notes |
|---|---|---|---|
| 1 | `sleepmedic.co` landing serves 200 | PASS | curl -L returns 200 |
| 2 | `/blog/` index serves 200 + post list renders | PASS | 200; posts-index.json is source of truth |
| 3 | `/app/` page serves 200 + form present | PASS | GA4 tag + form submit wired to Pi `/app-interest` |
| 4 | `/blog/feed.xml` serves 200 + valid RSS | PASS | Generated by `generate-rss-feed.mjs` |
| 5 | Pi `/health` returns ok | PASS | `{"status":"ok","subscribers":1}` |
| 6 | Pi `/subscribe` E2E (POST -> stored -> Resend send) | PASS | Smoke test subscriber persisted; subscribers count incremented |
| 7 | Pi `/app-interest` E2E (POST -> stored -> Discord ping) | PASS | Smoke test `{"ok":true,"clicks":2}` |
| 8 | Discord webhook alive | PASS | Pi -> Discord POST returns 204 |
| 9 | pm2 + cloudflared survive reboot (systemd enabled) | PASS | Both services `enabled` in systemd |
| 10 | Weekly pipeline autonomous (cron + failure notifications) | PASS | GH Actions cron Mon+Thu 9am MT; failure path opens issue + emails |

## Known gaps (non-blocking)

- **GA4 Key Events not yet marked.** The new web stream `G-717M9L2RTM` was created 2026-04-15. Key events (`app_interest_click`, `app_interest_email`, `newsletter_subscribe`, `blog_post_view`) can only be marked AFTER GA4 observes them from real traffic. See `ANALYTICS.md` for exact steps, do this ~24h after first real traffic.
- **GA4 custom dimensions created but no data yet.** `Location`, `Slug`, `Category` are registered against parameters that fire on every event. Will populate as traffic flows.
- **Old measurement ID `G-5H4073EG26` fully replaced.** 22 files updated with `G-717M9L2RTM`. Zero remaining references.
- **GoatCounter remains as dual-analytics source.** Per user decision (2026-04-15): keep alongside GA4.

## Smoke tests re-runnable with

```bash
curl -sSL -o /dev/null -w '%{http_code}\n' https://sleepmedic.co/
curl -sSL -o /dev/null -w '%{http_code}\n' https://sleepmedic.co/blog/
curl -sSL -o /dev/null -w '%{http_code}\n' https://sleepmedic.co/app/
curl -sSL -o /dev/null -w '%{http_code}\n' https://sleepmedic.co/blog/feed.xml
curl -sS https://pi.sleepmedic.co/health
```
